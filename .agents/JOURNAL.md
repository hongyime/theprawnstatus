# Journal

2026-09-13 — Revalidated delayed GitHub schedule creation. Separate scheduling reliability from successful job execution; do not increase writes until storage and failure-recovery contracts are measured. Preserve all prior records and owner choices.

2026-09-13 — Implemented immutable batches, deduplicated target configurations, atomic current projection and the original status_runs compatibility API. Real SQL fixtures reproduced and fixed a configuration race and UTC-window mismatch; streaming fixtures reproduced and fixed two response-cancellation leaks. Added a 400 MB collection guard and a Node manual recovery path sharing the Edge collector's lease contract. Original observations and summaries are retained. Local contracts, app checks and capacity fixtures pass; hosted CI and production cutover remain pending. See SCHEDULER.md for exact release order and recovery constraints.

2026-09-13 — Merged PR #19 after hosted checks passed, deployed the app and Edge collector, and applied the transactionally recorded storage migration with original data fingerprints intact. The hosted auth check rejected the API-supplied service JWT at the collector's exact-match check. Keep platform JWT verification and introduce a dedicated private collector header instead of binding request authentication to the platform database credential. No collector schedule or atomic GitHub variable is enabled. A second accelerated projection-update burst added 5.2 MB after vacuum, exceeding the initial reuse expectation; the capacity guard and usage monitoring remain necessary.

2026-09-13 — PR #20 passed hosted checks and merged, but the Edge-secret setting API rejected the new setting with HTTP 403. The generated Vault record is unused and no schedule is enabled. Authorize the existing service JWT after Supabase's signature-verification gateway, with strict project, role, issuer and expiry claims, so no new Edge-secret permission is needed. The gateway's signature verification is mandatory and covered by configuration and forged-token tests. Live checks confirmed forged signatures fail at the gateway while a valid service JWT reaches the existing handler. The runtime database credential may differ from the caller's service token.
