# Journal

2026-09-13 — Revalidated delayed GitHub schedule creation. Separate scheduling reliability from successful job execution; do not increase writes until storage and failure-recovery contracts are measured. Preserve all prior records and owner choices.

2026-09-13 — Implemented immutable batches, deduplicated target configurations, atomic current projection and the original status_runs compatibility API. Real SQL fixtures reproduced and fixed a configuration race and UTC-window mismatch; streaming fixtures reproduced and fixed two response-cancellation leaks. Added a 400 MB collection guard and a Node manual recovery path sharing the Edge collector's lease contract. Original observations and summaries are retained. Local contracts, app checks and capacity fixtures pass; hosted CI and production cutover remain pending. See SCHEDULER.md for exact release order and recovery constraints.

2026-09-13 — Merged PR #19 after hosted checks passed, deployed the app and Edge collector, and applied the transactionally recorded storage migration with original data fingerprints intact. The hosted auth check rejected the API-supplied service JWT at the collector's exact-match check. Keep platform JWT verification and introduce a dedicated private collector header instead of binding request authentication to the platform database credential. No collector schedule or atomic GitHub variable is enabled. A second accelerated projection-update burst added 5.2 MB after vacuum, exceeding the initial reuse expectation; the capacity guard and usage monitoring remain necessary.

2026-09-13 — PR #20 passed hosted checks and merged, but the Edge-secret setting API rejected the new setting with HTTP 403. The generated Vault record is unused and no schedule is enabled. Authorize the existing service JWT after Supabase's signature-verification gateway, with strict project, role, issuer and expiry claims, so no new Edge-secret permission is needed. The gateway's signature verification is mandatory and covered by configuration and forged-token tests. Live checks confirmed forged signatures fail at the gateway while a valid service JWT reaches the existing handler. The runtime database credential may differ from the caller's service token.

2026-09-13: Supabase Cron and atomic storage are enabled; the 13:00 and 13:10 UTC runs each committed one batch of 22 observations. The 13:05 request failed before acquiring a lease: Supabase edge logs show HTTP 504 on claim_status_collection, returned as collector HTTP 503. Original sample, archived-summary and health fingerprints still match. A real full rebuild and desktop/mobile history checks pass. The follow-up retries HTTP 502/503/504 once after 250 ms within the original 15-second database deadline, using the exact same body. An uncertain claim can return busy without duplicate probes; identical commit retries use the existing idempotence contract. Authorization/rate-limit failures and invalid successful response bodies are not retried.

2026-09-14 09:35 SGT emergency: status-uptime-5m Cron job 1 is now INACTIVE after the Vercel team reached 100% of its Fluid Active CPU allowance. This stops automatic external website probes; existing observations, the enabled collector lease and daily rebuild are retained. GitHub STATUS_COLLECTION_BACKEND remains atomic, so its recurring fallback jobs are skipped. Do not reactivate five-minute polling until CPU attribution and a budgeted cadence are reviewed. Historical data remains in Supabase. See the portfolio incident report; scheduled requests removed are not measured CPU savings.

2026-09-22/23 — Diagnosed and fixed the Vercel Hobby-tier 10GB storage crisis: sgSHIOK2026's `.vercelignore` was un-ignoring a 5.25GB generated-data folder that got bundled on every `vercel deploy` CLI run (happened 3x); deleted the 2 stale CLI deployments, kept production. spritescavenger had 1.78GB of committed `public/icons/` (16,548 files) re-bundling on every push; built `.github/workflows/vercel-cleanup.yml` here plus a per-project `.github/workflows/vercel-deploy-cleanup.yml` in spritescavenger itself that deletes all other deployments the moment the new one for that exact commit (matched by githubCommitSha) goes READY — tested live end to end, went from unbounded accumulation to exactly 1 deployment, site verified still serving 200. `scripts/cleanup-deployments.ps1` here runs org-wide weekly (Sun 3am UTC) as a backstop, keep=5 per project, never touches current production. First live run: kept 129, deleted 203 across 27 projects. Do not use Node fetch/AbortController for Vercel API polling on this machine — it hangs indefinitely; PowerShell Invoke-RestMethod/Invoke-WebRequest works reliably.
