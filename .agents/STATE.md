# Project State

2026-09-13: Active portfolio pass on uptime scheduling reliability. Production source 8d834853 preserves raw samples and displayed history, but GitHub schedule events arrive hours apart. First measure the real write/egress footprint; prepare lossless storage and an authenticated, bounded, idempotent collector before enabling reliable five-minute scheduling. Existing records, environment files, health auditing and every portfolio requirement remain preserved. No production mutation in this setup. Task list: .agents/handoffs/status-scheduler-20260913.json.

PR #19 is merged as 4e03491 and Vercel production is READY. Hosted CI passed all 15 checks. Migration 20260913100307 is applied; all 14,808 existing samples, 679 archived summaries and 35 rows in each health table retain their fingerprints. Edge collector v1 is deployed with JWT verification, but the hosted auth check rejected the supplied service JWT before any probes or collector writes. Scheduling and the atomic GitHub variable remain disabled. The follow-up separates the private collector request secret from the platform-managed database credential. Two regression cases reproduce the old header behaviour. Next: pass updated local/hosted contracts, configure the dedicated Edge/Vault secret without logging it, redeploy, verify disabled calls, then follow SCHEDULER.md. Preserve all archives and batches. The second projection burst also exceeded the initial space-reuse estimate; its measured 5.2 MB extra growth is documented rather than hidden.
