# Project State

2026-09-13: Active portfolio pass on uptime scheduling reliability. Production source 8d834853 preserves raw samples and displayed history, but GitHub schedule events arrive hours apart. First measure the real write/egress footprint; prepare lossless storage and an authenticated, bounded, idempotent collector before enabling reliable five-minute scheduling. Existing records, environment files, health auditing and every portfolio requirement remain preserved. No production mutation in this setup. Task list: .agents/handoffs/status-scheduler-20260913.json.

Local implementation is checked: 85 unit tests plus the added capacity case, build, lint, Deno entrypoint and 27 browser checks pass. Repeatable SQL, parity and real Deno/PostgREST HTTP contracts pass, including manual Node recovery, preserved legacy inserts/readers, lease contention, transaction rollback, a 400 MB capacity guard and automatic RLS after public helper grants are removed. Synthetic 30-day storage and 90-day rebuild benchmarks are documented in SCHEDULER.md. No production schema, Edge function, repository variable or Cron change has occurred. Next: review CI on the release source, then perform the fingerprinted migration and staged scheduler cutover in SCHEDULER.md. Preserve all existing archives and batches during recovery.
