# Project State

2026-09-13: Active portfolio pass on uptime scheduling reliability. Production source 8d834853 preserves raw samples and displayed history, but GitHub schedule events arrive hours apart. First measure the real write/egress footprint; prepare lossless storage and an authenticated, bounded, idempotent collector before enabling reliable five-minute scheduling. Existing records, environment files, health auditing and every portfolio requirement remain preserved. No production mutation in this setup. Task list: .agents/handoffs/status-scheduler-20260913.json.

PR #19 and #20 are merged; migration 20260913100307 preserves all 14,808 original samples, 679 summaries and 35 rows in each health table. Edge collector v1 remains deployed and collection is disabled. The dedicated Edge-secret setting was rejected with HTTP 403; its generated Vault secret is unused. The final auth follow-up uses Supabase's mandatory JWT-signature gateway and restricts verified claims to the project's unexpired service-role token. Live checks prove the gateway rejects a forged signature before the handler. Local fixtures cover invalid signatures, ordinary users, wrong projects and separate caller/runtime credentials. Next: finish hosted checks on this follow-up, merge/redeploy, prove authorized disabled calls and rejected unprivileged calls, then configure Vault/Cron and observe recurring slots. Keep all archives and batches. SCHEDULER.md also records the failed initial space-reuse estimate and the 400 MB guard.
